<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Available Courses</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            background-color: #1c1f3a;
            /* Same as studentProfile background */
            color: #fff;
            /* Text color for contrast */
        }

        h1 {
            text-align: center;
            color: #ecf0f1;
            /* Light gray for better visibility */
            margin: 20px 0;
            font-size: 2.5em;
        }

        .course-list {
            display: flex;
            flex-wrap: wrap;
            justify-content: center;
            gap: 20px;
            margin: 20px;
            padding: 0;
            list-style: none;
        }

        .course-card {
            background: #34495e;
            /* Slightly lighter than the page background */
            color: #ecf0f1;
            /* Text color for better readability */
            border-radius: 10px;
            padding: 20px;
            width: 300px;
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.3);
            /* Modern shadow effect */
            transition: transform 0.3s ease, box-shadow 0.3s ease;
        }

        .course-card:hover {
            transform: translateY(-10px);
            /* Slight lift on hover */
            box-shadow: 0 8px 20px rgba(0, 0, 0, 0.5);
            /* Deeper shadow on hover */
        }

        .course-card h2 {
            font-size: 22px;
            margin-bottom: 10px;
            color: #fff;
            /* A greenish accent color */
        }

        .course-card p {
            font-size: 14px;
            margin: 10px 0;
            color: #bdc3c7;
            /* Light gray for secondary text */
        }

        .course-card a {
            display: block;
            text-align: center;
            text-decoration: none;
            color: #fff;
            background: #1c1f3a;
            /* Matches the accent color */
            padding: 10px 15px;
            border-radius: 5px;
            font-size: 14px;
            font-weight: bold;
            transition: background 0.3s ease, transform 0.3s ease;
        }

        .course-card a:hover {
            background: #00bfff;
            /* Darker green on hover */
            transform: scale(1.05);
            /* Slight zoom effect */
        }
    </style>
</head>

<body>
    <h1>Available Courses</h1>
    <ul class="course-list">
        <% if (courses.length> 0) { %>
            <% courses.forEach(course=> { %>
                <li class="course-card">
                    <h2>
                        <%= course.name %>
                    </h2>
                    <p><strong>Description:</strong>
                        <%= course.desc %>
                    </p>
                    <p><strong>Duration:</strong>
                        <%= course.duration %> weeks
                    </p>
                    <a href="/api/courses/courseDetails/<%= course.id %>">View Details</a>
                </li>
                <% }); %>
                    <% } else { %>
                        <p style="text-align: center; font-size: 18px;">No courses available at the moment. Please check
                            back later.</p>
                        <% } %>
    </ul>
</body>

</html>