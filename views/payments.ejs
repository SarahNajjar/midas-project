<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Student Payments</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #1c1f3a;
            color: #eaeaea;
            margin: 0;
            padding: 0;
        }

        .container {
            max-width: 800px;
            margin: 20px auto;
            padding: 20px;
            background-color: #2c3e50;
            border-radius: 10px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.3);
        }

        h1 {
            color: #00bfff;
            text-align: center;
            margin-bottom: 20px;
        }

        table {
            width: 100%;
            border-collapse: collapse;
        }

        th,
        td {
            padding: 10px;
            text-align: left;
            border: 1px solid #ddd;
        }

        th {
            background-color: #292942;
            color: #6ab1d7;
        }

        tr:nth-child(even) {
            background-color: #2e2e3f;
        }

        tr:hover {
            background-color: #3e3e5f;
        }
    </style>
</head>

<body>
    <div class="container">
        <h1>Your Payments</h1>
        <table>
            <thead>
                <tr>
                    <th>Payment ID</th>
                    <th>Amount</th>
                    <th>Date</th>
                    <th>Method</th>
                    <th>Transaction ID</th>
                </tr>
            </thead>
            <tbody>
                <% if (payments && payments.length> 0) { %>
                    <% payments.forEach(payment=> { %>
                        <tr>
                            <td>
                                <%= payment.id %>
                            </td>
                            <td>$<%= payment.amount %>
                            </td>
                            <td>
                                <%= payment.date %>
                            </td>
                            <td>
                                <%= payment.method %>
                            </td>
                            <td>
                                <%= payment.transaction_id %>
                            </td>
                        </tr>
                        <% }) %>
                            <% } else { %>
                                <tr>
                                    <td colspan="5" style="text-align: center;">No payments found.</td>
                                </tr>
                                <% } %>
            </tbody>
        </table>
        <div style="text-align: center; margin-top: 20px;">
            <a href="/studentProfile" class="btn btn-primary">Back to Profile</a>
        </div>
    </div>
</body>

</html>