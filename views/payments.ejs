<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Student Payments</title>
    <style>
        body {
            font-family: 'Arial', sans-serif;
            background: linear-gradient(135deg, #1f1f3a, #292942);
            color: #eaeaea;
            margin: 0;
            padding: 0;
        }

        .container {
            max-width: 900px;
            margin: 50px auto;
            padding: 30px;
            background: #2e3440;
            border-radius: 15px;
            box-shadow: 0 10px 20px rgba(0, 0, 0, 0.4);
            text-align: center;
        }

        h1 {
            color: #8fbcbb;
            font-size: 2.5rem;
            margin-bottom: 20px;
            text-shadow: 0 2px 4px rgba(0, 0, 0, 0.6);
        }

        .notice {
            font-size: 1.1rem;
            color: #bf616a;
            margin-bottom: 20px;
            background: #3b4252;
            padding: 10px;
            border-radius: 10px;
        }

        table {
            width: 100%;
            border-collapse: collapse;
            margin-bottom: 20px;
        }

        th,
        td {
            padding: 15px;
            text-align: center;
            border: none;
        }

        th {
            background: #3b4252;
            color: #88c0d0;
            font-size: 1.1rem;
            text-transform: uppercase;
        }

        td {
            background: #4c566a;
            color: #d8dee9;
            font-size: 1rem;
        }

        tr:nth-child(odd) td {
            background: #434c5e;
        }

        tr:hover td {
            background: #5e81ac;
            color: #ffffff;
        }

        .no-payments {
            font-size: 1.2rem;
            color: #bf616a;
        }

        .btn-primary {
            display: inline-block;
            padding: 12px 25px;
            margin-top: 20px;
            background: #81a1c1;
            color: #ffffff;
            text-decoration: none;
            font-size: 1rem;
            border-radius: 25px;
            transition: background 0.3s ease, transform 0.3s ease;
        }

        .btn-primary:hover {
            background: #5e81ac;
            transform: scale(1.1);
            box-shadow: 0 8px 16px rgba(0, 0, 0, 0.4);
        }

        footer {
            margin-top: 30px;
            font-size: 0.9rem;
            color: #88c0d0;
        }

        footer a {
            color: #81a1c1;
            text-decoration: none;
        }

        footer a:hover {
            text-decoration: underline;
        }
    </style>
</head>

<body>
    <div class="container">
        <h1>Your Payments</h1>
        <div class="notice">
            <p>Please note: Payment is due within 48 hours of enrollment. Ensure timely payments to secure your enrollment.</p>
        </div>
        <table>
            <thead>
                <tr>
                    <th>Payment ID</th>
                    <th>Amount</th>
                    <th>Date</th>
                    <th>Method</th>
                    <th>Transaction ID</th>
                </tr>
            </thead>
            <tbody>
                <% if (payments && payments.length> 0) { %>
                    <% payments.forEach(payment=> { %>
                        <tr>
                            <td>
                                <%= payment.payment_id %>
                            </td>
                            <td>$<%= payment.payment_amount %>
                            </td>
                            <td>
                                <%= new Date(payment.payment_date).toLocaleDateString() %>
                            </td>
                            <td>
                                <%= payment.payment_method %>
                            </td>
                            <td>
                                <%= payment.payment_transaction_id %>
                            </td>
                        </tr>
                        <% }); %>
                            <% } else { %>
                                <tr>
                                    <td colspan="5" class="no-payments">No payments found.</td>
                                </tr>
                                <% } %>
            </tbody>
        </table>
    </div>
    <footer>
        <p>© 2023 MusicMaster. All rights reserved. | <a href="/privacy-policy">Privacy Policy</a></p>
    </footer>
</body>

</html>
