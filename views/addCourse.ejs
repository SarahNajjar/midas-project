<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Add Course</title>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600&display=swap" rel="stylesheet">
    <style>
        body {
            font-family: 'Inter', sans-serif;
            background-color: #1c1f3a;
            color: #eaeaea;
            margin: 0;
            padding: 0;
        }

        .container {
            width: 50%;
            margin: 50px auto;
            background: #2c3e50;
            padding: 20px;
            border-radius: 10px;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.4);
        }

        h1 {
            color: #00bfff;
            text-align: center;
            margin-bottom: 20px;
        }

        form {
            display: flex;
            flex-direction: column;
            gap: 15px;
        }

        input,
        select,
        textarea,
        button {
            padding: 10px;
            border: none;
            border-radius: 5px;
            font-size: 1rem;
        }

        input,
        select,
        textarea {
            background: #40444b;
            color: #eaeaea;
        }

        button {
            background: #7289da;
            color: #ffffff;
            cursor: pointer;
            transition: background 0.3s;
        }

        button:hover {
            background: #5a6aa3;
        }

        .cancel-button {
            background: #e74c3c;
            color: #ffffff;
            text-align: center;
            padding: 10px;
            border-radius: 5px;
            text-decoration: none;
            font-size: 1rem;
            transition: background 0.3s;
        }

        .cancel-button:hover {
            background: #c0392b;
        }
    </style>
</head>

<body>
    <div class="container">
        <h1>Add Course</h1>
        <form action="/api/courses/add-course" method="POST">
            <input type="text" name="name" placeholder="Course Name" required>
            <input type="text" name="instrument" placeholder="Instrument" required>
            <textarea name="desc" rows="4" placeholder="Description"></textarea>
            <input type="number" name="duration" placeholder="Duration (weeks)" required>
            <input type="text" name="schedule" placeholder="Schedule" required>
            <textarea name="prerequisities" rows="3" placeholder="Prerequisites"></textarea>

            <label for="instructor">Instructor</label>
            <select name="instructor_id" id="instructor" required>
                <option value="">Select Instructor</option>
                <% instructors.forEach(instructor=> { %>
                    <option value="<%= instructor.instructor_id %>">
                        <%= instructor.instructor_name %>
                    </option>
                    <% }); %>
            </select>

            <div style="display: flex; justify-content: space-between;">
                <button type="submit">Add Course</button>
                <a href="/api/courses/manageCourses" class="cancel-button">Cancel</a>
            </div>
        </form>
    </div>
</body>

</html>