<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Error</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #0d1117;
            /* Matches the dark theme */
            color: #c9d1d9;
            /* Matches the light text color */
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
        }

        .error-container {
            text-align: center;
            background-color: #161b22;
            /* Matches the form container color */
            padding: 30px;
            border-radius: 8px;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.5);
        }

        h1 {
            color: #ff6b6b;
            /* Red for error heading */
            font-size: 2.5rem;
        }

        p {
            font-size: 1.2rem;
            margin: 10px 0 20px;
        }

        a {
            color: #58a6ff;
            /* Link color matching the theme */
            text-decoration: none;
            font-weight: bold;
        }

        a:hover {
            text-decoration: underline;
        }

        .btn {
            padding: 10px 20px;
            background-color: #238636;
            /* Matches button color */
            border: none;
            border-radius: 5px;
            color: white;
            font-size: 1rem;
            font-weight: bold;
            cursor: pointer;
            transition: background-color 0.3s ease;
        }

        .btn:hover {
            background-color: #2ea043;
            /* Button hover color */
        }
    </style>
</head>

<body>
    <div class="error-container">
        <h1>Error</h1>
        <p>
            <%= message || "Something went wrong. Please try again later." %>
        </p>
        <a href="/" class="btn">Go to Homepage</a>
    </div>
</body>

</html>