<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>MusicMaster - Home</title>
    <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;700&display=swap" rel="stylesheet">
    <style>
        body,
        html {
            margin: 0;
            padding: 0;
            font-family: 'Inter', sans-serif;
            color: #eaeaea;
            background-color: #1c1f3a;
            /* Dark blue background */
        }

        .navbar-custom {
            background-color: #2c3e50;
            /* Darker blue */
        }

        .navbar-brand {
            color: #00bfff;
            /* Bright blue */
            font-weight: bold;
        }

        .navbar-custom .nav-link {
            color: #d0e7ff;
            /* Light blue */
        }

        .navbar-custom .nav-link:hover {
            color: #00bfff;
            /* Bright blue on hover */
        }

        .btn-primary {
            background-color: #3e8ed0;
            /* Medium blue */
            border: none;
            padding: 12px 30px;
            font-size: 1.1rem;
            border-radius: 50px;
            color: #ffffff;
            transition: background-color 0.3s, box-shadow 0.3s;
        }

        .btn-primary:hover {
            background-color: #1a6ea6;
            /* Darker blue */
            box-shadow: 0 8px 16px rgba(0, 0, 0, 0.3);
        }

        .hero-section {
            background-image: url('https://source.unsplash.com/1600x900/?music,lesson');
            background-size: cover;
            background-position: center;
            padding: 100px 0;
            color: #ffffff;
            /* White text */
            text-align: center;
            position: relative;
            background-attachment: fixed;
        }

        .hero-section::after {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: linear-gradient(to bottom, rgba(0, 0, 0, 0.5), rgba(0, 0, 0, 0.3));
            z-index: -1;
        }

        .hero-section h1 {
            font-size: 4rem;
            font-weight: 700;
            color: #00bfff;
            /* Bright blue */
            animation: fadeIn 1.5s ease-in;
        }

        @keyframes fadeIn {
            from {
                opacity: 0;
                transform: translateY(-20px);
            }

            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        .hero-section p {
            font-size: 1.5rem;
            margin-bottom: 20px;
            color: #d0e7ff;
            /* Light blue */
        }

        .features-section {
            padding: 50px 0;
            background-color: #2c3e50;
            /* Darker blue */
        }

        .features-section h2 {
            text-align: center;
            margin-bottom: 40px;
            color: #00bfff;
            /* Bright blue */
        }

        .feature-item {
            text-align: center;
            margin-bottom: 30px;
            padding: 20px;
            background-color: #3e4a6d;
            /* Mid-dark blue */
            border-radius: 10px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.3);
            transition: transform 0.3s ease, box-shadow 0.3s ease, opacity 0.3s ease;
            color: #ffffff;
            transform: scale(1);
        }

        .feature-item:hover {
            transform: scale(1.1);
            /* Slightly grow the box */
            box-shadow: 0 6px 12px rgba(0, 0, 0, 0.4);
            /* Deeper shadow */
        }

        .feature-item i {
            font-size: 3rem;
            color: #00bfff;
            /* Bright blue */
            margin-bottom: 15px;
        }

        .feature-item h4 {
            font-size: 1.5rem;
            margin-bottom: 10px;
        }

        .footer-custom {
            background: linear-gradient(to right, #2c3e50, #1e3a8a);
            /* Gradient */
            color: #eaeaea;
            padding: 20px 0;
            text-align: center;
        }

        .footer-custom a {
            color: #00bfff;
            /* Bright blue */
            text-decoration: none;
        }

        .footer-custom a:hover {
            text-decoration: underline;
        }
    </style>
</head>

<body>
    <nav class="navbar navbar-expand-lg navbar-custom">
        <div class="container">
            <a class="navbar-brand" href="#">MusicMaster</a>
            <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav"
                aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav ml-auto">
                    <li class="nav-item active">
                        <a class="nav-link" href="/">Home</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/login">Login</a>
                    </li>
                    <li class="nav-item">
                        <a class="btn btn-primary" href="/register">Register</a>
                    </li>
                </ul>
            </div>
        </div>
    </nav>

    <section class="hero-section">
        <h1>Discover Your Musical Potential</h1>
        <p>Join MusicMaster to master your musical skills and connect with top educators.</p>
        <a class="btn btn-primary" href="/register">Get Started</a>
    </section>

    <div class="container features-section">
        <h2>Why Choose MusicMaster?</h2>
        <div class="row">
            <div class="col-md-4">
                <div class="feature-item">
                    <i class="fas fa-guitar"></i>
                    <h4>Expert Instructors</h4>
                    <p>Learn from the best music teachers with years of experience.</p>
                </div>
            </div>
            <div class="col-md-4">
                <div class="feature-item">
                    <i class="fas fa-music"></i>
                    <h4>Diverse Courses</h4>
                    <p>Courses available for all skill levels, from beginners to pros.</p>
                </div>
            </div>
            <div class="col-md-4">
                <div class="feature-item">
                    <i class="fas fa-play-circle"></i>
                    <h4>Interactive Lessons</h4>
                    <p>Enjoy interactive, hands-on lessons that make learning fun.</p>
                </div>
            </div>
        </div>
    </div>

    <footer class="footer-custom">
        <p>© 2023 MusicMaster. All rights reserved. | <a href="/privacy-policy">Privacy Policy</a></p>
    </footer>

    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.0.5/dist/umd/popper.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
    <script src="https://kit.fontawesome.com/a076d05399.js"></script>
</body>

</html>