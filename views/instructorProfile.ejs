<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>MusicMaster - Instructor Profile</title>
    <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;700&display=swap" rel="stylesheet">
    <style>
        body {
            font-family: 'Inter', sans-serif;
            background-color: #1c1f3a;
            color: #eaeaea;
        }

        .navbar-custom {
            background-color: #2c3e50;
        }

        .navbar-brand {
            color: #00bfff;
            font-weight: bold;
        }

        .navbar-custom .nav-link {
            color: #d0e7ff;
        }

        .navbar-custom .nav-link:hover {
            color: #00bfff;
        }

        .profile-header {
            background: linear-gradient(135deg, #2c3e50, #1c1f3a);
            padding: 20px;
            text-align: center;
            color: #00bfff;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.3);
        }

        .profile-header h1 {
            margin: 0;
            font-size: 2.5rem;
        }

        .profile-header p {
            margin: 5px 0;
            font-size: 1.2rem;
            color: #d0e7ff;
        }

        .profile-container {
            margin: 20px auto;
            padding: 20px;
            background-color: #2c3e50;
            border-radius: 10px;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.3);
            max-width: 800px;
        }

        .profile-container h2 {
            color: #00bfff;
            font-size: 1.8rem;
            border-bottom: 2px solid #81c784;
            padding-bottom: 5px;
            margin-bottom: 15px;
        }

        .profile-info p {
            font-size: 1.1rem;
            line-height: 1.6;
            margin: 10px 0;
        }

        .btn-primary {
            background-color: #3e8ed0;
            border: none;
            padding: 10px 20px;
            font-size: 1rem;
            border-radius: 50px;
            color: #ffffff;
            transition: background-color 0.3s, box-shadow 0.3s;
        }

        .btn-primary:hover {
            background-color: #1a6ea6;
            box-shadow: 0 8px 16px rgba(0, 0, 0, 0.3);
        }

        footer {
            background-color: #2c3e50;
            color: #eaeaea;
            text-align: center;
            padding: 10px 0;
        }

        footer a {
            color: #00bfff;
            text-decoration: none;
        }

        footer a:hover {
            text-decoration: underline;
        }
    </style>
</head>

<body>
    <!-- Navbar -->
    <nav class="navbar navbar-expand-lg navbar-custom">
        <div class="container">
            <a class="navbar-brand" href="/">MusicMaster</a>
            <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav"
                aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav ml-auto">
                    <li class="nav-item">
                        <a class="nav-link" href="/">Home</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/login">Logout</a>
                    </li>
                </ul>
            </div>
        </div>
    </nav>

    <!-- Profile Header -->
    <div class="profile-header">
        <h1>Welcome, <%= user.first_name %>!</h1>
        <p>Your Role: Instructor</p>
    </div>

    <!-- Profile Information -->
    <div class="profile-container">
        <h2>Profile Information</h2>
        <div class="profile-info">
            <p><strong>Instructor ID:</strong>
                <%= user.id %>
            </p>
            <p><strong>Full Name:</strong>
                <%= user.first_name %>
                    <%= user.last_name %>
            </p>
            <p><strong>Email:</strong>
                <%= user.email %>
            </p>
            <p><strong>Specialization:</strong> Guitar, Piano, Violin</p>
        </div>
        <p>Here are your courses:</p>
        <ul>
            <% if (courses && courses.length> 0) { %>
                <% courses.forEach(course=> { %>
                    <li>
                        <%= course.name %> - <%= course.instrument %>
                    </li>
                    <% }) %>
                        <% } else { %>
                            <li>No courses found.</li>
                            <% } %>
        </ul>
    </div>

    <!-- Footer -->
    <footer>
        <p>© 2023 MusicMaster. All rights reserved. | <a href="/privacy-policy">Privacy Policy</a></p>
    </footer>

    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.0.5/dist/umd/popper.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
</body>

</html>